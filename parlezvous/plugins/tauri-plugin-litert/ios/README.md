# LiteRT iOS bridge

This Swift package keeps the shared Tauri plugin surface available on iOS. LiteRT-LM inference is not implemented on iOS yet, so model lifecycle calls report unavailable and the app routes AI generation through its server-backed provider.
