# Supertonic iOS bridge

This Swift package keeps the shared Tauri plugin surface available on iOS. On-device Supertonic synthesis is not implemented on iOS yet, so readiness reports unavailable and the app uses its configured server-backed TTS provider.
